extends layout

block content
  h1= title
  p Navya Muttukuri Lab10 #{title}
  br
  button(type="button", class="btn btn-dark",onclick="doBump()") Press Me
  br
  br
  div(id="bump") bump the count
  script.
    var bump_count=0;
    function doBump() {
    bump_count=bump_count+1;
    document.getElementById("bump").innerHTML = "bump " +bump_count+ " times";
   } 
  br
  br
   a(href='pet') Go to pet search
